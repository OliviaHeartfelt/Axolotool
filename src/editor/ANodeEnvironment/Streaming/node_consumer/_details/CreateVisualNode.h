#pragma once

#include "../../node_streamer/STNodeStreamer.h"

namespace STNodeConsumerDetails::CreateVisualNode {

    void createVisualNode(QGraphicsScene* scene, const STNodeStreamer::Config::FullNodePayload& payload) {
        // Build your ANode / QGraphicsItem and add to QGraphicsScene
    }
}